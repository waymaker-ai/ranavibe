# CoFounder CI/CD Guardrails
#
# Scans your codebase for AI security, compliance, cost, and safety issues.
# Runs on every PR and push to main. Posts findings as a PR comment and
# creates a GitHub Check Run with SARIF results.
#
# Configuration: Place a .cofounder.yml in your repo root (optional).

name: CoFounder Guard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  aicofounder-scan:
    name: CoFounder Security & Compliance Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # ── CoFounder Scan ─────────────────────────────────────────────────
      - name: Run CoFounder scan
        id: cofounder
        run: |
          npx @aicofounder/cli scan \
            --path . \
            --format sarif \
            --output aicofounder-results.sarif \
            --fail-on high \
            --config .cofounder.yml
        continue-on-error: true

      # ── Upload SARIF to GitHub Code Scanning ──────────────────────
      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: aicofounder-results.sarif
          category: aicofounder-guard

      # ── PR Comment with results ───────────────────────────────────
      - name: Post PR comment
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx @aicofounder/cli scan \
            --path . \
            --format github-pr \
            --config .cofounder.yml \
            --github-pr ${{ github.event.pull_request.number }}

      # ── Summary ───────────────────────────────────────────────────
      - name: Console summary
        if: always()
        run: |
          npx @aicofounder/cli scan \
            --path . \
            --format console \
            --config .cofounder.yml

      # ── Fail if CoFounder scan found high/critical issues ──────────────
      - name: Check scan result
        if: steps.cofounder.outcome == 'failure'
        run: |
          echo "::error::CoFounder scan found high or critical severity issues. See the PR comment and SARIF results for details."
          exit 1

{
  "name": "@waymakerai/aicofounder-adversarial",
  "version": "0.1.0",
  "private": true,
  "description": "Adversarial prompt suite for CoFounder. Measures how well an agent + CoFounder resists prompts designed to cause secret leaks, mock-data shipping, scope creep, design-system violations, and unauthorized refactors.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "run:baseline": "tsx src/harness.ts --mode=baseline",
    "run:guarded": "tsx src/harness.ts --mode=guarded",
    "run:compare": "tsx src/harness.ts --mode=compare"
  },
  "dependencies": {
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
